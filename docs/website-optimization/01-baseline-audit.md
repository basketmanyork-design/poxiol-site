# Baseline audit — 2026-09-15

- Repository: `https://github.com/basketmanyork-design/poxiol-site.git`; review base `b7840691cd176f57ed591961be2ec4413acb6739`, the deployed Reddit guide release recorded in the project log. Current isolated branch is clean before implementation. The unrelated project root and older `poxiol-site-build` worktree have uncommitted work and are untouched.
- Framework: Next 15.5.21, React 18, static export by default; `next.config.mjs` allows OpenNext as an optional build mode. Sanity Studio is under `studio/`, reads via `lib/sanity/`; Wrangler project name `poxiol-site`. Canonical host is `https://www.poxiol.com/`.
- Inquiry: project forms post directly to the approved Formspree endpoint via `NEXT_PUBLIC_FORMSPREE_CONTACT_ENDPOINT`. The current form accepts research-stage quantity, optional deadline and country, no postcode, and requires email. A 2xx response is treated as success without reading provider confirmation. Existing client prevents a double click while sending and warns on uncertain response; it cannot prove provider-side de-duplication or storage.
- Media: six supplied 1122×1402 PNGs (1–6) visually map to soccer, basketball, baseball, short-sleeve training, running/track, and full warm-up set. They are AI design illustrations. Supplied video `POXIOL_Hero_22s_Web_720p.mp4` is 4,644,204 bytes; frames/codec pending visual and technical inspection.
- Current homepage: seven sections with copy visually hidden on desktop behind an image containing text and transparent hit-zone CTAs. There are no six independent product cards. Procurement fields and buyer parity differ from V1.1.
- Existing category routes: soccer `/products/soccer-jerseys/`, basketball `/products/basketball-uniforms/`, baseball `/custom-baseball-softball-uniforms/`, training `/products/training-wear/`. Running/track and distinct warm-up landing routes are absent.
- Analytics, notifications, OKKI, live Sanity editing, current Production HTTP and Core Web Vitals: NOT_TESTED at baseline. No real form submission is authorized. Preview and Production checks will be recorded separately.
