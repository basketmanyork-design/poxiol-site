# Ad landing and product-intent map

This is a future campaign mapping only. No ad was published, and `AD_TRAFFIC_READY=NO` until the Production receiving chain is verified.

| Theme / supplied card | URL | Product context and buyer intent | Current validation |
|---|---|---|---|
| Soccer `(1)` | `/products/soccer-jerseys/` | Soccer uniforms → editable project/quote inquiry | Local route/output check pending report |
| Basketball `(2)` | `/products/basketball-uniforms/` | Basketball uniforms → editable project/quote inquiry | Local route/output check pending report |
| Baseball `(3)` | `/custom-baseball-softball-uniforms/` | Baseball uniforms → editable project/quote inquiry | Local route/output check pending report |
| Training `(4)` | `/products/training-wear/` | Training sets → editable project/quote inquiry | Local route/output check pending report |
| Running `(5)` | `/products/running-track-uniforms/` | Running/track uniforms → editable project inquiry | Local route/output check pending report |
| Warm-up `(6)` | `/products/warm-up-wear/` | Warm-up wear → editable project inquiry | Local route/output check pending report |
| Hero film | `/` | General teams and brands → project, mockup, sample, quote | Local nine-module check pending report |

The destination must match the pictured garment. Campaign text cannot infer real stock, exact MOQ, speed or free shipping from the design illustration. Before any ads, confirm the final URL and UTM source, mobile form, legal/analytics choice, notification handoff and duplicate behavior on the actual Production receiver.
